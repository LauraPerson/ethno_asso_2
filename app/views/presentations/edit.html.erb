<div class="container">
  <%= simple_form_for((@presentation), data: {turbo: false}) do |f| %>
    <%= f.input :name, placeholder: "Titre", label: false  %>
    <%= f.input :content, as: :rich_text_area, placeholder: "Contenu", label: false   %>
    <%= f.submit "Publier", class:"btn-primary"%>
  <% end %>
</div>
